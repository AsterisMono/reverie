# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
name: reverie-nvidia
description: the quiet workstation (with nvidia support)

base-image: ghcr.io/ublue-os/silverblue-nvidia
image-version: 44

modules:
  - from-file: reverie-base.yml
